<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.timelinelist">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.EditPerfilActivity"></activity>
        <activity android:name=".activities.CadastroActivity" />
        <activity android:name=".activities.EstatisticasActivity" />
        <activity android:name=".activities.PerfilActivity" />
        <activity android:name=".activities.ListaActivity" />
        <activity android:name=".activities.PesquisaActivity" />
        <activity android:name=".activities.LoadingActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity
            android:name=".activities.DetalheSerieActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen" />
        <activity
            android:name=".activities.DetalheFilmeActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>